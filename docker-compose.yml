services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./instance:/home/deployer/data
    environment:
      - PORT=8000
      - DATABASE_URL=sqlite:////home/deployer/data/swimmers.db
